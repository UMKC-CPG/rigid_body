# Pseudocode

> **Document hierarchy:** VISION → ARCHITECTURE → DESIGN → **PSEUDOCODE**
> → Code. For the design rationale behind these algorithms, see `DESIGN.md`.

---

<!-- Write key algorithms in language-agnostic pseudocode. Each section
should correspond to a major function or subsystem described in DESIGN.md.
Use consistent notation for variables. -->

## 1. Main Loop

```
function main():
    # Replace with your project's top-level flow.
    initialize()
    for each step:
        process()
    finalize()
```

---

## 2. Algorithm Name

```
function algorithm_name(inputs):
    # Describe the algorithm step by step.
    ...
    return outputs
```
