"""Shared pytest fixtures.

Add project-wide fixtures here. For example, a fixture that sets up the
path to your source directory:

    import sys, os

    _src = os.path.join(
        os.path.dirname(__file__), '..', 'src')
    sys.path.insert(0, os.path.abspath(_src))
"""
